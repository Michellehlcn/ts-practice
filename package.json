{
  "name": "backend-challenge",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./build && tsc",
    "build:watch": "tsc -w -p .",
    "dev": "nodemon ./build/startup.js",
    "start": "NODE_ENV=production node build/startup.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.5.1",
    "nodemon": "^3.0.1",
    "prettier": "^3.0.2",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "lodash": "^4.17.21",
    "lodash.debounce": "^4.0.8"
  }
}
